Code Coverage Tool
==================

コードカバレッジを計測します。

setup
-----
npm install

test
----
npm test

注意
----
Windows では `"test": "istanbul cover node_modules/mocha/bin/_mocha"` としないと istanbul が動かない不具合がある。